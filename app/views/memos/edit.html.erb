タイトル：<%= @book.title %><br />
メモ：<%= @book.memo %><br />
購入日: <%= @book.purchased_on %><br />
<br />
<%= form_for @memo, :url => book_memo_path(:book_id => @book.id, :id => @memo.id), :method => :put do |f| %>
  メモ：<%= f.text_field :body %><br />
  <%= f.submit %><br />
<% end %>
